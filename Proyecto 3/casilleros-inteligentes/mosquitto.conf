# Config de desarrollo para Mosquitto.
# Escucha en todas las interfaces para que la ESP32 (en la misma WiFi) pueda
# conectarse a la IP LAN del computador. Sin autenticacion (solo desarrollo).
listener 1883 0.0.0.0
allow_anonymous true
